<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_place_holder"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/welcome_screen_login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/welcome_screen_register_button"
        android:layout_alignLeft="@+id/welcome_screen_register_button"
        android:layout_alignRight="@+id/welcome_screen_register_button"
        android:text="@string/login_button_text"
        android:onClick="welcomeLoginClicked" />

    <Button
        android:id="@+id/welcome_screen_register_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="75dp"
        android:text="@string/register_button_text"
        android:onClick="welcomeRegisterClicked" />

</RelativeLayout>